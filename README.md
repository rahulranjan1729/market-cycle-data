# Market Cycle Data

Public, browser-readable market snapshot for the private Market Cycle Dashboard.

- `market-data.json` is refreshed from the NSE Indices daily snapshot every weekday after market close.
- The workflow can also be run manually from GitHub Actions.
- This repository contains data ingestion code only; the dashboard source remains private.
